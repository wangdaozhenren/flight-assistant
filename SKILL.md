---
name: flight-assistant
slug: flight-assistant
displayName: 机票助手
description: 机票比价助手——同航线同日期，比含税总价。当用户想比机票价格、机票哪里便宜、航班比价、特价机票、低价日历、flight price comparison 时使用。覆盖：携程、同程、去哪儿、飞猪、航司官网。航司+时段+舱位筛选，跨平台比价，低价日历与提前购票周期建议，退改政策差异标注。需出发地/目的地/日期信息。只做比价，不代订票、不代付款。
summary: "机票比价助手——同航线同日期，比含税总价。当用户想比机票价格、机票哪里便宜、航班比价、特价机票、低价日历、flight …"
version: 1.0.0
license: MIT
tags: 机票比价,特价机票,航班,携程,同程,去哪儿,飞猪,低价日历,含税价,退改,flight-comparison,机票助手
---

# 机票助手

同一航线、同一日期，比**含税总价**，退改政策同看。

## 工作流

1. **解析需求**：提取 `出发地`、`目的地`、`出发日期`、`返程日期（往返时）`、`人数`、可选 `航司偏好`、`时段偏好`、`舱位`。缺出发地/目的地/日期时先问一次——机票价格随日期剧烈波动。

2. **平台路由**：默认去哪儿 → 飞猪 → 携程 → 同程 → 航司官网（比直营价时）；用户点名平台时以用户为准。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：搜索航线 → 读前 5~10 条航班。每条记录至少含：`平台`、`航班号/航司`、`起降时间`、`机型`、`含税价`、`舱位`、`行李额`、`退改规则`、`链接`。

4. **登录/验证移交**：登录墙、验证码、滑块出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/flight-assistant-knowledge.md`）**：
   - **比含税总价**：展示价含不含税、含不含燃油/机场建设费，统一为"含税总价"口径；
   - **同航班同时段**：同一航班（航班号）在平台间比；不同航班按用户时段/航司偏好筛选后比；
   - **舱位/行李额**：经济舱不同舱位代码（Y/B/H/K…）价差与退改规则不同，标注舱位与行李额；
   - **退改政策**：特价票"不退不改" vs 全价票"免费退改"，同价不同政策要提示。

6. **低价日历**（用户要求时）：跨平台查看前后 3~7 天价格，给出"提前 X 天购票通常更优"的日历建议（基于页面实读，不编造规律）。

7. **报告输出**：Markdown 表 `平台 | 航班 | 起降 | 含税价 | 舱位/行李 | 退改 | 链接 | 备注`，结论给出"最低含税价渠道 + 政策提醒"。**每个价格必须附可点击的订票链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者透明）。

8. **盯价提醒**（可选）：用户说"这班降到 X 提醒我"时写入盯价清单（读 `doubao-cron-scheduler/SKILL.md`）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单航线 ≤2 次/日、两次页面操作间隔 ≥10 秒、导航后停留 3~8 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代订票、不代付款；价格页面实读，禁止编造。

## 边界声明

- 机票价格实时浮动（舱位库存随时变化），报告标注"价格截至 <读取时点>"，以下单页为准。
- 中转 vs 直飞、不同航司服务（餐食/行李）不同，报告按用户需求标注差异。
- 退改规则以航司/平台页面为准，报告提示"购票前确认退改与行李条款"。
